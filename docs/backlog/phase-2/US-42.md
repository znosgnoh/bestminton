# US-42: Extend Member Profile for Rankings and Tokens

**Epic:** Data Foundation

**As a** system,
**I want** each member to store Elo rating, match stats, and token balance,
**so that** challenges and bets can be tracked per player.

## Acceptance Criteria

- [ ] `Member` gains: `eloRating` (default `1200`), `totalMatches` (default `0`), `totalWins` (default `0`), `tokenBalance` (default `0`).
- [ ] Existing members receive defaults on migration; no data loss for Phase 1 fields.
- [ ] New members created via `/management` inherit the same defaults.
- [ ] API responses for members include the new fields where relevant.
